// @flow

import { SheetResponse } from "./post-message";
declare type SheetListType = "SINGLE_SELECTION";
declare type SheetListItem = {
  id: string,
  title?: string,
  description?: string,
};
declare type SheetUIElement = {
  id: string,
  type: "LIST",
  listType: SheetListType,
  autoSubmit?: boolean,
  selectedIds: Array<string>,
  items: Array<SheetListItem>,
};
declare type SheetUI = {
  title?: string,
  subtitle?: string,
  description?: string,
  content: Array<SheetUIElement>,
};
declare export var bottomSheet: (payload: SheetUI) => Promise<SheetResponse>;
declare export var bottomSheetSingleSelector: (x: {
  title?: string | void,
  subtitle?: string | void,
  description?: string | void,
  selectedId?: string | void,
  items: Array<SheetListItem>,
}) => Promise<{
  action: "SUBMIT" | "DISMISS",
  selectedId: string,
}>;
declare export {};
