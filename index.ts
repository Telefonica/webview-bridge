export {nativeConfirm, nativeAlert, nativeMessage} from './src/native-dialogs';

export {
    requestSimIcc,
    requestSimImsi,
    requestDeviceImei,
    attachToEmail,
    setWebViewTitle,
} from './src/native-utils';
